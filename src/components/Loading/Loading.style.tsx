import styled from 'styled-components';
import { rem } from 'polished';

export const Loading = styled.button`
  margin-top: -${rem(20)};
  margin-left: -${rem(20)};
  position: fixed;
  top: 50%;
  left: 50%;
  cursor: auto;
`;
